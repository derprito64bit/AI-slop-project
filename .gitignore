# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local screenshots from npm run shots
.shots

# Local secrets. The backend's own .env lives in its repo (TheKeems/UniServer),
# not here.
.env
.env.local

# Per-machine Claude settings. Ignored globally on some machines and not others,
# which is how one of these ended up committed.
.claude/settings.local.json
